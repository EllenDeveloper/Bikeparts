name: bikeparts-squid-proxy

services:
  squid:
    image: ubuntu/squid:5.2-22.04_beta
    container_name: bikeparts-squid-proxy
    ports:
      - "3128:3128"
    volumes:
      - ./squid.conf:/etc/squid/squid.conf
      - squid-cache:/var/spool/squid

volumes:
  squid-cache:
  